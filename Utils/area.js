var area = {
    circle : function(radius){ return (Math.PI * Math.pow(radius, 2)) },

    rectangle : function(length, breadth) { return length * breadth },

    cylinder : function(radius, height)  { return (Math.PI * Math.pow(radius, 2) * height) }
}


